// Backend base URL -- deployed on Railway, publicly reachable over HTTPS.
// No more LAN IP / USB tunnel dependency: works over any network, including
// mobile data, since the phone no longer needs to reach this PC at all.
export const API_BASE = 'https://sonorus-app-production.up.railway.app';
